<!DOCTYPE html>

<% link_to "Gmaps", google_path %>

<html>
    <head>
    </head>
    <body>
        <style>
        #map {
            width: 100%;
            height: 400px;
        }
        </style>
    <h3>Google Maps Demo</h3>
    <!--The Div element for the map -->
    <div id="map"></div>
    <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDQaAprLTdxv6WAYiy5SZSOkQ-C97fcBSs&callback=initMap"></script>
    <script> 


        function initMap() {
        let uluru = {lat: 39.8283, lng: -98.5795};
        let map = new google.maps.Map(
            document.getElementById('map'), {zoom: 4, center: uluru}
        );
        let marker = new google.maps.Marker({position: uluru, map: map})};

        var informationArray = <%= raw @info_array %> ;


    // function createMarkers(address) {
    //     let marker = new google.maps.Marker({positiion: address, map: map})
    // };
    //     // creates marker for an address
    //   function grabAddresses {
    //     informationArray.foreach((address) => {
    //         address = [informationArray.lat, informationArray.lng];
    //         createMarkers(address);
    //     });
    //     }
        
        // passes every address into create markers function
            

            // sets up the map
    </script>
    </body>
</html>

    <%# function allAdress() {
        allAddress.all.push(class Address);
    }
        allAddress.all = []; %>

    <%# //  allAddress.all.forEach(address => {
    //         createMarkers(address)
    //     }); %>